export const metadata = {
  title: 'Synthesis',
  description:
    'Learn about the Synthesis Ghostwriter agent for multi-source collaborative research across private Knowledge Graphs.',
}

# Synthesis (Ghostwriter)

Synthesis is a multi-source agent capable of querying across private Knowledge Graphs for collaborative research. It enables AI agents and researchers to compile comprehensive reports from multiple independent creators while ensuring proper attribution and royalties. {{ className: 'lead' }}

<Note>
  **Status**: Synthesis is currently in development. The API endpoints below
  represent the planned implementation.
</Note>

## Overview

Synthesis (codenamed "Ghostwriter") breaks down the traditional research workflow:

```
Traditional Research:
Creator A's Blog → Creator B's Article → Creator C's Paper → Write Report
(You must subscribe to all, manually synthesize)

Synthesis:
AI queries multiple KGs → Compiles report → Pays each creator → You get synthesis
```

## What is Synthesis?

Synthesis is a **collaborative research engine** that:

1. **Queries multiple private Knowledge Graphs** simultaneously
2. **Aggregates facts and findings** from different creators
3. **Maintains proper attribution** throughout
4. **Generates comprehensive reports** with citations
5. **Handles payments** to all contributing creators

## The Synthesis Request Model

### Properties

<Properties>
  <Property name="id" type="string">
    Unique identifier for the synthesis request.
  </Property>
  <Property name="query" type="string">
    The research query or topic.
  </Property>
  <Property name="sources" type="array">
    List of creator addresses to query.
  </Property>
  <Property name="depth" type="enum">
    Research depth: `quick`, `standard`, `comprehensive`.
  </Property>
  <Property name="status" type="enum">
    Status: `pending`, `processing`, `completed`, `failed`.
  </Property>
  <Property name="created_at" type="timestamp">
    When the request was created.
  </Property>
  <Property name="completed_at" type="timestamp">
    When the synthesis was completed.
  </Property>
  <Property name="total_fee" type="string">
    Total fee for the synthesis (base + query royalties).
  </Property>
</Properties>

---

## Create Synthesis Request {{ tag: 'POST', label: '/v1/synthesis/request' }}

<Row>
  <Col>

    Create a new synthesis request for collaborative research.

    ### Required attributes

    <Properties>
      <Property name="query" type="string">
        The research topic or question.
      </Property>
      <Property name="sources" type="array">
        Array of creator Sui addresses to query from.
      </Property>
    </Properties>

    ### Optional attributes

    <Properties>
      <Property name="depth" type="enum">
        Research depth: `quick` (1-2 sources), `standard` (3-5), `comprehensive` (5+).
      </Property>
      <Property name="specific_triples" type="array">
        Pre-identified triple IDs to include.
      </Property>
    </Properties>

    ### Fee structure

    - **Synthesis Fee**: Flat fee based on depth ($0.01 - $0.10)
    - **Query Royalties**: Per-triple fees (80% to creators)

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/v1/synthesis/request">

    ```bash {{ title: 'cURL' }}
    curl https://api.zing.protocol/v1/synthesis/request \
      -H "Authorization: Bearer {jwt_token}" \
      -d query="What are the latest developments in AI safety research?" \
      -d sources='["0xcreator...alice", "0xcreator...bob", "0xcreator...carol"]' \
      -d depth="standard"
    ```

    ```js
    const request = await client.synthesis.create({
      query: 'What are the latest developments in AI safety research?',
      sources: [
        '0xcreator...alice',
        '0xcreator...bob',
        '0xcreator...carol',
      ],
      depth: 'standard',
    });
    ```

    ```json {{ title: 'Response' }}
    {
      "id": "synth_abc123",
      "query": "What are the latest developments in AI safety research?",
      "sources": [
        "0xcreator...alice",
        "0xcreator...bob",
        "0xcreator...carol"
      ],
      "depth": "standard",
      "status": "pending",
      "estimated_fee": "0.05",
      "breakdown": {
        "synthesis_base": "0.01",
        "query_royalties_est": "0.04"
      },
      "created_at": 1709312400
    }
    ```

    </CodeGroup>

  </Col>
</Row>

---

## Get Synthesis Result {{ tag: 'GET', label: '/v1/synthesis/:id' }}

<Row>
  <Col>

    Get the result of a synthesis request.

    ### Path parameters

    <Properties>
      <Property name="id" type="string">
        The synthesis request ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/synthesis/synth_abc123">

    ```bash {{ title: 'cURL' }}
    curl https://api.zing.protocol/v1/synthesis/synth_abc123 \
      -H "Authorization: Bearer {jwt_token}"
    ```

    ```js
    const result = await client.synthesis.get('synth_abc123');
    ```

    ```json {{ title: 'Response (Completed)' }}
    {
      "id": "synth_abc123",
      "query": "What are the latest developments in AI safety research?",
      "status": "completed",
      "result": {
        "summary": "This report synthesizes findings from 3 creators on AI safety...",
        "sections": [
          {
            "heading": "Current Approaches",
            "content": "Based on triple data from creators...",
            "citations": [
              {
                "creator": "0xcreator...alice",
                "blob_id": "walrus_blob_abc123",
                "attribution": "@alice's article on AI safety"
              }
            ]
          }
        ],
        "total_facts_used": 45,
        "unique_sources": 3
      },
      "citations": [
        {
          "creator": "0xcreator...alice",
          "triples_used": 15,
          "fee": "0.015"
        },
        {
          "creator": "0xcreator...bob",
          "triples_used": 18,
          "fee": "0.018"
        },
        {
          "creator": "0xcreator...carol",
          "triples_used": 12,
          "fee": "0.012"
        }
      ],
      "total_fee": "0.055",
      "completed_at": 1709313000
    }
    ```

    </CodeGroup>

  </Col>
</Row>

---

## List Synthesis History {{ tag: 'GET', label: '/v1/synthesis/history' }}

<Row>
  <Col>

    Get the history of synthesis requests for the authenticated user.

    ### Query parameters

    <Properties>
      <Property name="status" type="enum">
        Filter by status: `pending`, `processing`, `completed`, `failed`.
      </Property>
      <Property name="limit" type="integer">
        Number of results (default: 10).
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/synthesis/history">

    ```bash {{ title: 'cURL' }}
    curl https://api.zing.protocol/v1/synthesis/history \
      -H "Authorization: Bearer {jwt_token}"
    ```

    ```js
    const history = await client.synthesis.history();
    ```

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "synth_abc123",
          "query": "AI safety research developments",
          "sources_count": 3,
          "depth": "standard",
          "status": "completed",
          "total_fee": "0.055",
          "created_at": 1709312400
        },
        {
          "id": "synth_xyz789",
          "query": "Web3 gaming trends",
          "sources_count": 5,
          "depth": "comprehensive",
          "status": "processing",
          "estimated_fee": "0.12",
          "created_at": 1709308800
        }
      ]
    }
    ```

    </CodeGroup>

  </Col>
</Row>

---

## Synthesis Flow

```
1. User submits research query
         ↓
2. System identifies relevant creators/sources
         ↓
3. For each source:
   ┌─────────────────────────────────────┐
   │  PPQ Query via /v1/ppq/initiate     │
   └─────────────────────────────────────┘
         ↓
4. Aggregate all triples
         ↓
5. Generate synthesis with citations
         ↓
6. Calculate total fees (base + royalties)
         ↓
7. User pays all fees atomically
         ↓
8. Attribution records created for all
         ↓
9. Synthesis delivered with full citations
```

## Fee Structure

| Depth             | Synthesis Base Fee | Max Query Royalties |
| ----------------- | ------------------ | ------------------- |
| **Quick**         | $0.01              | $0.05               |
| **Standard**      | $0.02              | $0.10               |
| **Comprehensive** | $0.05              | $0.25               |

### Example Calculation

```
Query: "Latest AI safety developments"
Sources: 3 creators (alice, bob, carol)
Depth: standard

Synthesis base: $0.02
Query royalties:
  - alice: 15 triples × $0.001 = $0.015
  - bob: 18 triples × $0.001 = $0.018
  - carol: 12 triples × $0.001 = $0.012

Total: $0.02 + $0.045 = $0.065
Creator total: $0.036 (80% of $0.045)
```

## Citation Format

Each fact in the synthesis includes citation:

```json
{
  "fact": "New interpretability techniques emerge in 2024",
  "confidence": 0.92,
  "citations": [
    {
      "creator": "0xcreator...alice",
      "attribution": "@alice",
      "blob_id": "walrus_blob_abc123",
      "triple_id": "triple_042",
      "fee_paid": "0.001"
    }
  ]
}
```

## Use Cases

| Use Case                    | Description                                  |
| --------------------------- | -------------------------------------------- |
| **Research Papers**         | Compile findings from multiple experts       |
| **Market Analysis**         | Aggregate insights from multiple analysts    |
| **Technical Documentation** | Synthesize tutorials from different creators |
| **Due Diligence**           | Gather information from multiple sources     |
| **Trend Reports**           | Combine perspectives on emerging topics      |

## Creator Benefits

| Benefit         | Description                                        |
| --------------- | -------------------------------------------------- |
| **New Revenue** | Earn royalties from synthesis queries              |
| **Exposure**    | Get featured in research across topics             |
| **Low Effort**  | Content automatically included (opt-out available) |
| **Attribution** | Full citation for every fact used                  |

## Next Steps

- [Pay-Per-Query API](/pay-per-query) - Individual triple access
- [Attribution Ledger](/attribution-ledger) - Track usage attribution
- [Knowledge Graph](/knowledge-graph) - Explore available triples

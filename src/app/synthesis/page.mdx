export const metadata = {
  title: 'Synthesis',
  description:
    'Learn about the Synthesis engine for multi-source collaborative research across indexed content with attribution.',
}

# Synthesis

Synthesis is a multi-source research engine capable of querying across indexed wikis for collaborative research. It enables AI agents and researchers to compile comprehensive reports from multiple independent creators while ensuring proper attribution and royalties. {{ className: 'lead' }}

<Note>
  **Status**: Synthesis is currently in development. The API endpoints below
  represent the planned implementation.
</Note>

## Overview

Synthesis breaks down the traditional research workflow:

```
Traditional Research:
Creator A's Blog → Creator B's Article → Creator C's Paper → Write Report
(You must subscribe to all, manually synthesize)

Synthesis:
AI queries multiple indexes → Compiles report → Pays each creator → You get synthesis
```

## What is Synthesis?

Synthesis is a **collaborative research engine** that:

1. **Queries multiple creator indexes** simultaneously
2. **Aggregates content chunks** from different creators
3. **Maintains proper attribution** throughout
4. **Generates comprehensive reports** with citations
5. **Handles payments** to all contributing creators

## The Synthesis Request Model

### Properties

<Properties>
  <Property name="id" type="string">
    Unique identifier for the synthesis request.
  </Property>
  <Property name="query" type="string">
    The research query or topic.
  </Property>
  <Property name="sources" type="array">
    List of creator addresses to query.
  </Property>
  <Property name="depth" type="enum">
    Research depth: `quick`, `standard`, `comprehensive`.
  </Property>
  <Property name="status" type="enum">
    Status: `pending`, `processing`, `completed`, `failed`.
  </Property>
  <Property name="created_at" type="timestamp">
    When the request was created.
  </Property>
  <Property name="completed_at" type="timestamp">
    When the synthesis was completed.
  </Property>
  <Property name="total_fee" type="string">
    Total fee for the synthesis (base + chunk retrieval fees).
  </Property>
</Properties>

---

## Create Synthesis Request {{ tag: 'POST', label: '/v1/synthesis/request' }}

<Row>
  <Col>

    Create a new synthesis request for collaborative research.

    ### Required attributes

    <Properties>
      <Property name="query" type="string">
        The research topic or question.
      </Property>
      <Property name="sources" type="array">
        Array of creator Sui addresses to query from.
      </Property>
    </Properties>

    ### Optional attributes

    <Properties>
      <Property name="depth" type="enum">
        Research depth: `quick` (1-2 sources), `standard` (3-5), `comprehensive` (5+).
      </Property>
      <Property name="specific_chunks" type="array">
        Pre-identified chunk IDs to include.
      </Property>
    </Properties>

    ### Fee structure

    - **Synthesis Fee**: Flat fee based on depth ($0.01 - $0.10)
    - **Chunk Retrieval**: Per-chunk token-based fees (80% to creators)

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/v1/synthesis/request">

    ```bash {{ title: 'cURL' }}
    curl https://api.prod.zing.you/v1/synthesis/request \
      -H "Content-Type: application/json" \
      -d '{
        "query": "What are the latest developments in AI safety research?",
        "sources": ["0xcreator...alice", "0xcreator...bob", "0xcreator...carol"],
        "depth": "standard"
      }'
    ```

    ```json {{ title: 'Response' }}
    {
      "id": "synth_abc123",
      "query": "What are the latest developments in AI safety research?",
      "sources": [
        "0xcreator...alice",
        "0xcreator...bob",
        "0xcreator...carol"
      ],
      "depth": "standard",
      "status": "pending",
      "estimated_fee": "0.05",
      "breakdown": {
        "synthesis_base": "0.01",
        "chunk_retrieval_est": "0.04"
      },
      "created_at": 1709312400
    }
    ```

    </CodeGroup>

  </Col>
</Row>

---

## Get Synthesis Result {{ tag: 'GET', label: '/v1/synthesis/:id' }}

<Row>
  <Col>

    Get the result of a synthesis request. Returns the compiled report with citations.

    ### Path parameters

    <Properties>
      <Property name="id" type="string">
        The synthesis request ID.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/synthesis/synth_abc123">

    ```bash {{ title: 'cURL' }}
    curl https://api.prod.zing.you/v1/synthesis/synth_abc123
    ```

    ```json {{ title: 'Response (Completed)' }}
    {
      "id": "synth_abc123",
      "query": "What are the latest developments in AI safety research?",
      "status": "completed",
      "result": {
        "summary": "This report synthesizes findings from 3 creators on AI safety...",
        "sections": [
          {
            "heading": "Current Approaches",
            "content": "Based on indexed content from creators...",
            "citations": [
              {
                "creator": "0xcreator...alice",
                "blob_id": "walrus_blob_abc123",
                "attribution": "@alice's article on AI safety"
              }
            ]
          }
        ],
        "total_chunks_used": 45,
        "unique_sources": 3
      },
      "citations": [
        {
          "creator": "0xcreator...alice",
          "chunks_used": 15,
          "fee": "0.015"
        },
        {
          "creator": "0xcreator...bob",
          "chunks_used": 18,
          "fee": "0.018"
        },
        {
          "creator": "0xcreator...carol",
          "chunks_used": 12,
          "fee": "0.012"
        }
      ],
      "total_fee": "0.055",
      "completed_at": 1709313000
    }
    ```

    </CodeGroup>

  </Col>
</Row>

---

## List Synthesis History {{ tag: 'GET', label: '/v1/synthesis/history' }}

<Row>
  <Col>

    Get the history of synthesis requests for the authenticated user.

    ### Query parameters

    <Properties>
      <Property name="status" type="enum">
        Filter by status: `pending`, `processing`, `completed`, `failed`.
      </Property>
      <Property name="limit" type="integer">
        Number of results (default: 10).
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/synthesis/history">

    ```bash {{ title: 'cURL' }}
    curl https://api.prod.zing.you/v1/synthesis/history
    ```

    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "id": "synth_abc123",
          "query": "AI safety research developments",
          "sources_count": 3,
          "depth": "standard",
          "status": "completed",
          "total_fee": "0.055",
          "created_at": 1709312400
        },
        {
          "id": "synth_xyz789",
          "query": "Web3 gaming trends",
          "sources_count": 5,
          "depth": "comprehensive",
          "status": "processing",
          "estimated_fee": "0.12",
          "created_at": 1709308800
        }
      ]
    }
    ```

    </CodeGroup>

  </Col>
</Row>

---

## Synthesis Flow

```
1. User submits research query
         ↓
2. System identifies relevant creators/sources
         ↓
3. For each source:
   ┌─────────────────────────────────────┐
   │  Search chunk index via /chunks     │
   └─────────────────────────────────────┘
         ↓
4. Aggregate all chunks
         ↓
5. Generate synthesis with citations
         ↓
6. Calculate total fees (base + chunk retrieval)
         ↓
7. User pays all fees atomically
         ↓
8. Attribution records created for all
         ↓
9. Synthesis delivered with full citations
```

## Fee Structure

| Depth             | Synthesis Base Fee | Max Chunk Retrieval |
| ----------------- | ------------------ | ------------------- |
| **Quick**         | $0.01              | $0.05               |
| **Standard**      | $0.02              | $0.10               |
| **Comprehensive** | $0.05              | $0.25               |

### Example Calculation

```
Query: "Latest AI safety developments"
Sources: 3 creators (alice, bob, carol)
Depth: standard

Synthesis base: $0.02
Chunk retrieval:
  - alice: 15 chunks × ~116 tokens avg × ($0.0004 + $0.0008)/1K = ~$0.015
  - bob: 18 chunks × ~116 tokens avg × ($0.0004 + $0.0012)/1K = ~$0.018
  - carol: 12 chunks × ~116 tokens avg × ($0.0004 + $0.0008)/1K = ~$0.012

Total: $0.02 + $0.045 = $0.065
Creator total: ~$0.036 (80% of $0.045)
```

## Citation Format

Each finding in the synthesis includes citations:

```json
{
  "finding": "New interpretability techniques emerged in 2024",
  "confidence": 0.92,
  "citations": [
    {
      "creator": "0xcreator...alice",
      "attribution": "@alice",
      "blob_id": "walrus_blob_abc123",
      "chunk_id": 42,
      "fee_paid": "0.0001"
    }
  ]
}
```

## Use Cases

| Use Case                    | Description                                  |
| --------------------------- | -------------------------------------------- |
| **Research Papers**         | Compile findings from multiple experts       |
| **Market Analysis**         | Aggregate insights from multiple analysts    |
| **Technical Documentation** | Synthesize tutorials from different creators |
| **Due Diligence**           | Gather information from multiple sources     |
| **Trend Reports**           | Combine perspectives on emerging topics      |

## Creator Benefits

| Benefit         | Description                                        |
| --------------- | -------------------------------------------------- |
| **New Revenue** | Earn royalties from synthesis chunk retrieval      |
| **Exposure**    | Get featured in research across topics             |
| **Low Effort**  | Content automatically indexed (opt-out available)  |
| **Attribution** | Full citation for every chunk used                  |

## Next Steps

- [Search API](/knowledge-graph) - Explore available search and chunk APIs
- [Pay-Per-Query API](/pay-per-query) - Per-chunk retrieval pricing
- [Attribution Ledger](/attribution-ledger) - Track usage attribution
